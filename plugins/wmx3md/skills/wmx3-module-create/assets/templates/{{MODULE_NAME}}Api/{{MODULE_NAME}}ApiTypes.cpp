/*******************************************************************************
*
* {{MODULE_NAME}}ApiTypes.cpp
*
* C++ API 클래스 생성자 정의
* wmx3Api 네임스페이스 내 데이터 타입 클래스들의 기본 초기화
*
*******************************************************************************/

#pragma warning(disable:4351)

#include <string.h>
#include "{{MODULE_NAME}}Api.h"

/*
 * 여기에 각 C++ 클래스의 생성자 구현
 *
 * 패턴 예시:
 *
 * wmx3Api::{{MODULE_NAME}}Status::{{MODULE_NAME}}Status() :
 *     state(),
 *     someValue(),
 *     errorFlag()
 * {
 * }
 *
 * wmx3Api::{{MODULE_NAME}}Options::{{MODULE_NAME}}Options() :
 *     someOption(),
 *     anotherOption()
 * {
 * }
 *
 * union 타입의 경우 memset으로 초기화:
 *
 * wmx3Api::{{MODULE_NAME}}SomeUnionType::{{MODULE_NAME}}SomeUnionType()
 * {
 *     memset(this, 0x0, sizeof({{MODULE_NAME}}SomeUnionType));
 * }
 */

/* {{MODULE_NAME}}Status 생성자 */
wmx3Api::{{MODULE_NAME}}Status::{{MODULE_NAME}}Status()
    /* 여기에 멤버 초기화 목록 추가 */
    /* : state(), someValue() */
{
}
